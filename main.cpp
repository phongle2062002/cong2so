#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
using namespace std; 
#define PI 3.1416
int main(int argc, char** argv) {
	
	float a,b,x;
	cout<<"Nhap so a :";
	cin >> a;
	cout<<"Nhap so b :"; //Nh?p r t? bàn phím
	cin >> b; 
	x= a+b;
	cout<<"Tong 2 so "<<x;
	return 0;
}
